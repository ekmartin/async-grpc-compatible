syntax = "proto3";

package async.grpc.compatible.fixture;

message EchoRequest {
	string value = 1;
}

message EchoResponse {
	string value = 1;
}

service EchoService {
	rpc Echo(EchoRequest) returns (EchoResponse);
}
